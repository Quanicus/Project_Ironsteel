class Tile {
    constructor({
        type,
        frameSize, //(x, y) used to crop from sprite map
        targetFrame, //(x, y) used to specify top-left corner of where to crop on the sprite map
        elevation,
    }) {
        this.imgSource
    }
}